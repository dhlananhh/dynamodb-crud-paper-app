<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>
    Update Paper - <%= paper.paper_name %>
  </title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <link rel="stylesheet" href="styles.css">
  <style>
    button {
      transition: background-color 0.2s ease-in-out;
    }
  </style>
</head>

<body class="bg-gray-100 font-sans">
  <div class="container mx-auto p-4 sm:p-6 lg:p-8">
    <h1 class="mb-6 text-3xl font-bold text-center text-gray-800 uppercase">
      Update Paper Details
    </h1>

    <div class="bg-white border border-gray-300 rounded-lg shadow-md max-w-2xl mx-auto">
      <div class="p-6 space-y-6">
        <form action="/update/<%= paper.paper_id %>" method="POST" id="update-course" class="space-y-4">
          <div>
            <label for="paper_id" class="text-sm font-medium text-gray-700 block mb-1">Paper ID *</label>
            <input type="number" name="id_display" id="paper_id"
              class="bg-gray-200 border border-gray-300 text-gray-600 sm:text-sm rounded-lg focus:outline-none block w-full p-2.5 cursor-not-allowed"
              value="<%= paper.paper_id %>" readonly>
          </div>

          <div>
            <label for="paper_name" class="text-sm font-medium text-gray-700 block mb-1">Paper Name *</label>
            <input type="text" name="paper_name" id="paper_name"
              class="bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5"
              value="<%= paper.paper_name %>" required>
          </div>

          <div>
            <label for="authors" class="text-sm font-medium text-gray-700 block mb-1">Authors *</label>
            <input type="text" name="name" id="name"
              class="bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5"
              value="<%= paper.authors %>" required>
          </div>

          <div>
            <label for="isbn" class="text-sm font-medium text-gray-700 block mb-1">ISBN *</label>
            <input type="text" name="isbn" id="isbn"
              class="bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5"
              value="<%= paper.isbn %>" required>
          </div>

          <div>
            <label for="page_number" class="text-sm font-medium text-gray-700 block mb-1">Page number *</label>
            <input type="text" name="page_number" id="page_number"
              class="bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5"
              value="<%= paper.page_number %>" required>
          </div>

          <div>
            <label for="publish_year" class="text-sm font-medium text-gray-700 block mb-1">Publish year *</label>
            <input type="text" name="publish_year" id="publish_year"
              class="bg-gray-50 border border-gray-300 text-gray-900 sm:text-sm rounded-lg focus:ring-blue-500 focus:border-blue-500 block w-full p-2.5"
              value="<%= paper.publish_year %>" required>
          </div>

          <div class="flex justify-center space-x-4 pt-2">
            <button
              class="bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-6 rounded focus:outline-none focus:shadow-outline"
              type="submit">Save Changes</button>
            <a href="/"
              class="bg-gray-500 hover:bg-gray-600 text-white font-bold py-2 px-6 rounded focus:outline-none focus:shadow-outline no-underline">Cancel</a>
          </div>
        </form>
      </div>
    </div>
    <%- include('fragments/footer') %>
  </div>
</body>

</html>
